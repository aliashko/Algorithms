namespace DataStructures.Trees
{
    /// <summary>
    /// A Suffix Tree for a given text is a compressed trie for all suffixes of the given text
    /// Used for searching substrings
    /// Search complexity - O(m), where m = searchPattern.length
    /// Requires a lot of memory and resources for preprocessing of tree
    /// Siutable for more or less stable unchangabl texts
    /// </summary>
    public class SuffixTree
    {

    }
}
